// Placeholder de teste — o app usa canais nativos Android que não são testáveis em ambiente desktop.
// Testes de integração devem ser executados em um dispositivo ou emulador Android.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(true, isTrue);
  });
}
